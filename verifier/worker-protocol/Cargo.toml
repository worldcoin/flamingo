[package]
name = "flamingo-verifier-worker-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.0"
license = "MIT"
publish = false

# Standalone manifest: also consumed by the private worker.
[dependencies]
ciborium = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_bytes = "0.11"
thiserror = "2"
